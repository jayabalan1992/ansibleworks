---

- hosts: webserver
  tasks:

    - name: install the latest version of Nginx
      yum:
        name: nginx
        state: latest

    - name: start nginx
      service:
          name: nginx
          state: started
